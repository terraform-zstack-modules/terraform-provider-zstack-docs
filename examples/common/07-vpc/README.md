# 07-vpc

Creates a basic VPC L3 network with an initial subnet CIDR.

This example depends on existing L2 network and virtual router UUIDs. Confirm
these values in the ZStack console or query them with data sources before
applying.
